import { BiLoaderAlt } from 'react-icons/bi';

const Loader = () => (
  <div className="loader d-flex justify-content-center align-items-center">
    <BiLoaderAlt size={40} className="load-icon" />
  </div>
);

export default Loader;
